services:
  - type: web
    name: brain-cache-frontend
    env: static
    rootDir: Frontend
    buildCommand: npm install && npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
